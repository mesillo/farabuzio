#!/usr/bin/env node

for( let i=0 ; i<10 ; i++ ) {
    console.log( "==========================================" );
    console.log( "\t\t"+ i );
    console.log( "==========================================" );
}
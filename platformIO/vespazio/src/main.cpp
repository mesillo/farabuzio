#include <Arduino.h>

#include <rpmCounter.h>

void setup() {
	//RpmCounter::init();
	//RpmCounter::enable();
	//Serial.begin( 9600 );
}

void loop() {
	// put your main code here, to run repeatedly:
}